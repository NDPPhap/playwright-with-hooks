**# 1. Run the source in cmd**
- Run all the tests
  > npx playwright test
- Run with UI mode
  > npx playwright test --ui
- Run single test file
  > npx playwright test tests\test\homepage.test.ts
- Run a test with specific title
  > npx playwright test -g "Verify that the DSTA logo should be displayed"